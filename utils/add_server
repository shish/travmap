#!/bin/sh
cd `dirname $0`
. config.sh
mysql -u$MYSQL_USER -p$MYSQL_PASS -h $MYSQL_HOST $MYSQL_DB -e "INSERT INTO servers VALUES('$1', 'map', '$2', $3, 250, 250)"
./update_text.sh $1 map
./update_mysql.sh $1
rm ../cache/servers.txt
