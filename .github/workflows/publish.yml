name: Publish

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Set build vars
        run: |
          echo "BUILD_TIME=$(date -Iseconds)" >> $GITHUB_ENV
          echo "BUILD_HASH=$GITHUB_SHA" >> $GITHUB_ENV
      - name: Publish
        uses: elgohr/Publish-Docker-Github-Action@main
        with:
          name: ${{ vars.DOCKER_USERNAME }}/${GITHUB_REPOSITORY##*/}
          username: ${{ vars.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          buildargs: BUILD_TIME,BUILD_HASH

  deploy:
    if: github.ref == 'refs/heads/master'
    runs-on: ubuntu-24.04
    needs:
      - build
    steps:
      - name: Deploy
        uses: shish/watchtower-update@main
        with:
          url: https://watchtower.shish.io/v1/update
          api_token: ${{ secrets.WATCHTOWER_TOKEN }}
          images: ${{ vars.DOCKER_USERNAME }}/${GITHUB_REPOSITORY##*/}
